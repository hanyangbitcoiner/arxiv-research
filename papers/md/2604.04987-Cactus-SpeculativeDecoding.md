---
title: "Cactus: 约束接受Speculative Decoding"
authors: ["Yongchang Hao", "Lili Mou"]
date: 2026-04-09
arxiv: "2604.04987"
type: paper
tags:
  - 推理加速
  - 解码优化
  - 约束优化
  - AR模型
  - SpeculativeDecoding
---

# Cactus: 约束接受Speculative Decoding

**论文：** [arXiv:2604.04987](https://arxiv.org/abs/2604.04987)
**作者：** Yongchang Hao, Lili Mou (University of Alberta)
**日期：** 2026-04-05

---

## 核心问题

Speculative Sampling (SpS) 严格强制生成分布匹配验证器分布，但这过于严格。

---

## 核心贡献

1. **约束优化框架**：将 speculative sampling 形式化为约束优化问题
2. **Cactus 算法**：保证受控的分布偏离，同时提高接受率

---

## SpS vs Cactus

| 方面 | SpS | Cactus |
|------|-----|--------|
| 分布匹配 | 严格匹配 | 受控偏离 |
| 接受率 | 较低 | 更高 |
| 输出质量 | 无损 | 可控降级 |

---

## 伪代码实现

```python
class Cactus:
    def __init__(self, draft, verifier, delta=0.1):
        self.draft = draft
        self.verifier = verifier
        self.delta = delta

    def decode(self, prompt, max_len):
        x = prompt
        while len(x) < max_len:
            draft_tokens = self.draft.sample(x, n_tokens=10)
            q = self.verifier.get_probs(x)

            for t in draft_tokens:
                p_t = self.draft.get_prob(x, t)
                q_t = q[t].item()
                acceptance = self.compute_acceptance(p_t, q_t)
                if random.random() < acceptance:
                    x = x + [t]
                else:
                    x = x + [self.sample_recover(q, t)]
                    break
        return x

    def compute_acceptance(self, p, q):
        if p <= q:
            return min(1.0, p / q)
        else:
            gamma = self.find_gamma(p, q, self.delta)
            return min(1.0, gamma * p / q)
```

---

## 相关论文

- [[2604.06798-MoBiE-MoE二值化|MoBiE]] - 模型压缩
- [[2604.03957-BWTA-二值化Transformer|BWTA]] - 推理效率
