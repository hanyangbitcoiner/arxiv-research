---
title: "SEA: 自学习诊断Agent"
authors: ["Bingxuan Li", "Simo Du", "Yue Guo"]
date: 2026-04-09
arxiv: "2604.07269"
type: paper
tags:
  - Agent
  - 持续学习
  - 双记忆
  - 强化学习
  - 医学AI
---

# SEA: 自学习诊断Agent

**论文：** [arXiv:2604.07269](https://arxiv.org/abs/2604.07269)
**作者：** Bingxuan Li, Simo Du, Yue Guo (UIUC, Jacobi Medical Center)
**日期：** 2026-04-08

---

## 核心问题

临床诊断专家不仅依赖医学知识积累，更依赖从经验中形成的**可复用诊断模式**。现有 LLM 诊断 Agent 存在两个问题：

1. **记忆管理瓶颈**：每个案例独立处理，无法跨案例复用经验
2. **缺乏持续学习**：无法从反馈中自动改进

---

## 核心贡献

1. **双记忆架构**：短程记忆（案例）+ 长程记忆（规则）
2. **联合强化学习框架**：同时优化诊断能力和记忆管理
3. **可插拔设计**：无需修改模型参数即可提升性能

---

## 双记忆架构

```
┌─────────────────────────────────────────────────────────┐
│                    Agent Policy Model                      │
│  输入: 患者病例 xt                                         │
│  输出: 诊断结果 ot + 记忆操作 ut                            │
└─────────────────────────────────────────────────────────┘
                          │
         ┌────────────────┴────────────────┐
         ▼                                 ▼
┌─────────────────────┐         ┌─────────────────────┐
│   短程记忆 (MS)      │         │   长程记忆 (ML)      │
│   - 最近 K 个案例    │◄─pop─► │   - 抽象诊断规则    │
│   - append-only     │         │   - 从经验中提炼    │
│   - 有界容量        │         │   - 可检索应用      │
└─────────────────────┘         └─────────────────────┘
```

### 数学形式

```
MS^t = {c1, ..., c|MS^t|}, |MS^t| ≤ K
ML^t = {r1, ..., r|ML^t|}
```

### 记忆操作

```
ut ∈ {list, append, pop(p), consolidate}

- list:    检索当前案例和规则
- append:  将新案例加入短程记忆
- pop(p):  驱逐特定案例（容量超限时）
- consolidate: 将案例总结为规则存入长程记忆
```

---

## 奖励函数设计

```python
def compute_reward(self, action, diagnosis_correct, round_t, T):
    r_diag = 5 if diagnosis_correct else -5
    r_mem = -3 * len(self.MS) / self.K

    # 早期强调记忆形成，后期强调诊断
    lambda_diag = 1.0 * (round_t / T)
    lambda_mem = 1.0 * (1 - round_t / T)

    return lambda_diag * r_diag + lambda_mem * r_mem
```

---

## 伪代码实现

```python
class DualMemoryAgent:
    def __init__(self, policy_model, K=10):
        self.policy = policy_model
        self.K = K
        self.MS = []  # 短程案例记忆
        self.ML = []  # 长程规则记忆

    def update_memory(self, action, case, outcome):
        if action == 'append':
            self.MS.append({'case': case, 'outcome': outcome})
            if len(self.MS) > self.K:
                self.consolidate_oldest()
        elif action == 'consolidate':
            oldest = self.MS.pop(0)
            rule = self.summarize_to_rule(oldest)
            self.ML.append(rule)

    def summarize_to_rule(self, case_record):
        prompt = f"从案例中提炼诊断规则：{case_record}"
        return {'rule': self.policy.generate(prompt)}
```

---

## 实验结果

| 设置 | 方法 | 准确率 | 提升 |
|------|------|--------|------|
| 标准评估 | SEA (Qwen-8B) | **92.46%** | **+19.6%** |
| 长期任务 | SEA (Qwen-8B) | **72.14%** | **+0.35** |

---

## 关键洞察

1. **联合优化优于单独优化**：仅优化诊断正确率收益有限
2. **记忆需要结构**：简单添加记忆反而降低性能
3. **可插拔性**：双记忆模块可搭配任意基座模型

---

## 相关论文

- [[2604.07223-TraceSafe-安全护栏|TraceSafe]] - Agent 安全评估
- [[2604.04804-SkillX-技能知识库|SkillX]] - Agent 技能库构建
